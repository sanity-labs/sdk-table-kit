---
'@sanity-labs/sdk-table-kit': minor
---

Expose a generic `column.string()` helper that supports SDK role visibility, comment metadata, and auto-save integration for arbitrary string fields.

`column.title()` is now deprecated as a compatibility preset, allowing existing tables to keep working while documentation and new examples move to the generic string helper.
