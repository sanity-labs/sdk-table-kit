---
'@sanity-labs/sdk-table-kit': patch
---

Update the `@sanity-labs/react-table-kit` dependency range to `^1.3.0` so `sdk-table-kit` ships
against the released `column.string()` and server-grouping APIs.
